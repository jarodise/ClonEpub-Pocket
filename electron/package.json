{
    "name": "clonepub-pocket",
    "version": "0.1.0",
    "description": "Convert EPUB e-books to audiobooks with voice cloning",
    "main": "dist/main.js",
    "author": "jarodise",
    "license": "MIT",
    "scripts": {
        "start": "npm run build && electron .",
        "build": "tsc",
        "package": "npm run build && electron-forge package",
        "make": "npm run build && electron-forge make"
    },
    "devDependencies": {
        "@electron-forge/cli": "^7.2.0",
        "@electron-forge/maker-dmg": "^7.2.0",
        "@electron/rebuild": "^3.6.0",
        "@types/node": "^25.0.8",
        "electron": "^28.1.0",
        "typescript": "^5.3.3"
    },
    "dependencies": {
        "electron-log": "^5.1.0"
    }
}
